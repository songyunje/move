// 정렬되지 않은 배열에서
// 찾고자 하는 값의 index를 찾아 반환한다.
// 못찾은 경우 -1을 반환한다.

sequentialSearch(A, key)
	A ← 주어진 배열
	n ← 주어진 배열의 크기
	key ← 찾고자 하는 값
	
	i ← 0
	while(i < n and A[i] != key)
		i ← i+1
	
	if(i<n) return i
	else return -1
